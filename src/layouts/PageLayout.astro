---
import BaseLayout from './BaseLayout.astro';
import Header from '@/components/layout/Header.astro';
import Footer from '@/components/layout/Footer.astro';
import StickyCalculatorCTA from '@/components/ui/StickyCalculatorCTA.astro';
import type { Language } from '@/i18n/config';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  lang?: Language;
  noindex?: boolean;
  canonicalURL?: string;
}

const { lang = 'en', ...props } = Astro.props;
---

<BaseLayout lang={lang} {...props}>
  <slot name="head" slot="head" />

  <Header lang={lang} />

  <main class="flex-grow">
    <slot />
  </main>

  <Footer lang={lang} />

  <!-- Sticky Calculator CTA -->
  <StickyCalculatorCTA lang={lang} />

  <slot name="analytics" slot="analytics" />
</BaseLayout>
