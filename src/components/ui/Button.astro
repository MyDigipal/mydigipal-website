---
import { cn } from '@/lib/utils';

interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  class?: string;
  type?: 'button' | 'submit' | 'reset';
  disabled?: boolean;
  external?: boolean;
  icon?: boolean;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  class: className,
  type = 'button',
  disabled = false,
  external = false,
  icon = false,
} = Astro.props;

const baseStyles = 'inline-flex items-center justify-center gap-2 font-medium transition-all duration-250 ease-smooth disabled:opacity-50 disabled:cursor-not-allowed focus:outline-none focus:ring-2 focus:ring-offset-2';

const variants = {
  // Le bouton de l'accueil (25/09/2026) : le bleu du logo, en pilule, graisse 600.
  primary: 'bg-primary-600 text-white font-semibold hover:bg-primary-700 active:scale-[0.98] shadow-sm hover:shadow-medium focus:ring-primary-600',
  secondary: 'bg-white text-slate-900 font-semibold border border-slate-300 hover:border-slate-400 hover:bg-slate-50 active:scale-[0.98] focus:ring-slate-500',
  ghost: 'text-slate-600 hover:text-slate-900 hover:bg-slate-100 focus:ring-slate-500',
};

const sizes = {
  sm: 'px-4 py-2 text-sm rounded-full',
  md: 'px-6 py-3 text-base rounded-full',
  lg: 'px-8 py-4 text-lg rounded-full',
};

const iconSizes = {
  sm: 'p-2',
  md: 'p-3',
  lg: 'p-4',
};

const classes = cn(
  baseStyles,
  variants[variant],
  icon ? iconSizes[size] : sizes[size],
  icon && 'rounded-full',
  className
);

const externalProps = external ? { target: '_blank', rel: 'noopener noreferrer' } : {};
---

{href ? (
  <a href={href} class={classes} {...externalProps}>
    <slot />
  </a>
) : (
  <button type={type} class={classes} disabled={disabled}>
    <slot />
  </button>
)}
