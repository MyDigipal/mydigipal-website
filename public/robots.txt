# robots.txt for mydigipal.com
# https://mydigipal.com

User-agent: *
Allow: /

# Block access to internal/admin paths
Disallow: /api/
Disallow: /_astro/
Disallow: /admin/

# Safety net: client reports were once dropped in the assets folder and served
# publicly. They now live outside the build, in docs/client-reports/. This rule
# stays so a report copied back here is never crawled. Pattern rule on purpose:
# naming the files would publish their paths.
Disallow: /images/*.html$

# Block URL parameters (duplicate content prevention)
Disallow: /*?*

# Sitemap location (images are included in main sitemap)
Sitemap: https://mydigipal.com/sitemap.xml

# Crawl-delay for polite crawlers (optional, not all bots respect this)
# Crawl-delay: 1
